[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "edgeval-cu"
version = "0.1.0"
description = "GPU-accelerated edge detection evaluation (ODS/OIS/AP/R50)"
readme = "README.md"
authors = [
    { name = "0xrjman & Contributors" },
]
license = { text = "Apache-2.0" }
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Cython",
    "Programming Language :: CUDA",
    "Topic :: Scientific/Engineering :: Image Processing",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
keywords = ["edge-detection", "evaluation", "cuda", "gpu", "ods", "ois", "bsds500"]
requires-python = ">=3.8"
dependencies = [
    "numpy",
    "scipy>=1.6.0",
    "opencv-python",
    "tqdm",
    "click",
]

[project.urls]
Homepage = "https://github.com/0xrjman/edgeval.cu"
Repository = "https://github.com/0xrjman/edgeval.cu"

[project.scripts]
edgeval = "edgeval_cu.cli:cli"

[tool.setuptools.packages.find]
where = ["."]
include = ["edgeval_cu", "edgeval_cu.*"]

[tool.setuptools.package-data]
edgeval_cu = [
    "cxx/lib/solve_csa.so",
    "cuda/auction_cuda.so",
    "cuda/edge_builder.so",
]

[tool.setuptools]
zip-safe = false
